# 共通 STEP 数学・物理刷题 Web App：完整开发 Workflow

## 0. 项目目标与边界

开发一套面向日本大学入学共通测试数学与物理的手机优先 Web App。核心闭环是：学习模式逐空补全连续解答 → 记录具体错误步骤 → 模拟测试独立作答 → 分析知识与解题行为薄弱点 → 生成巩固题与延时复查 → 再次学习或模拟。

旧 LOGIKA 微信小程序仅作为视觉、布局、现有功能与内容管理逻辑的只读参考。不得修改原 ZIP，不迁移 `wx.*` API、云函数、下标答案或与微信身份体系绑定的实现。

首版采用本地 JSON 与 `localStorage`，通过 Repository 接口隔离数据来源；不接正式后端、不实现真实登录、支付、通知、社交或全国真实排名。

## 1. 每次工作的固定循环

```text
读取当前需求与控制文档
→ 检查已有代码和未提交变更
→ 更新实施计划
→ 修改代码
→ 运行类型、静态、单元与构建检查
→ 在浏览器中完成相关真实流程
→ 对照需求矩阵自我审查
→ 修复发现的问题并补回归测试
→ 重新运行全部相关检查
→ 写入阶段 checkpoint
```

不得只凭“页面能打开”宣称完成；不得用截图、不可操作按钮或与用户数据无关的写死统计冒充功能。

## 2. 阶段门禁

每阶段必须生成 `docs/checkpoints/phase-XX-review.md`，包含：目标、完成与未完成内容、主要文件、命令、自动测试、浏览器测试、已修复问题、剩余风险、需求矩阵证据以及 `PASS`/`FAIL`。

只有同时满足下列条件才可 PASS：

- TypeScript、ESLint、单元测试、相关 E2E 与 Production Build 通过；
- 没有未解决 P0/P1；
- 对应需求已有实现位置和测试证据；
- 浏览器关键流程真实可操作，刷新恢复规则经过验证。

FAIL 必须先修复，不得越过门禁。

## 3. 技术原则

- Vite + React + TypeScript + React Router；
- Zustand persist 管理本地会话；Zod 负责版本化题目校验；KaTeX 渲染公式；
- Vitest 做领域与组件测试，Playwright 做手机端流程测试；
- CSS Variables 建立设计系统，不引入大型 UI 组件库；
- 页面只依赖 Repository 接口，不直接依赖数据库实现；
- 题目与 Attempt 分离，所有答案、空格、设问使用稳定字符串 ID；
- 时间以 ISO 字符串保存，显示时采用 `Asia/Tokyo`。

## 4. 完整阶段

### 阶段 0：参考项目审计与工程初始化

只读审计旧小程序；记录页面、云函数、题库、缓存、可复用视觉与不可迁移耦合；建立独立 Vite 工程、依赖、目录、scripts、控制文档和 smoke test。门禁：`pnpm check`、`pnpm test:e2e`、原参考材料无修改。

### 阶段 1：需求锁定与信息架构

固化四个底部入口（问题、分析、排名、我的）、全部路由、学习与模拟的不同流程、返回/刷新/空态/错误态规则。更新 PRD、矩阵、架构和测试计划。

### 阶段 2：设计系统与页面框架

实现浅米色、深蓝黑、暗红错误、编号分区、实体按键式阴影、手机优先容器、AppShell、顶部与底部导航、基础状态组件和所有非空路由。验收 390×844、430×932、768×1024、1440×900，无横向溢出或导航遮挡。

### 阶段 3：题目 Schema 与内容渲染

实现 `Question`、内容块、学习空格、模拟设问、资源、分类、类题与版本；Zod 检查重复 ID、悬空答案/资源/空格/类题和分数；实现文字、LaTeX、图片、表格渲染；至少 2 道数学、2 道物理样例，覆盖图片、表格、长日文和 5 个以上空格。

### 阶段 4：学习模式答题引擎

实现 `unanswered → active → answered-correct/answered-wrong → reviewed`，保存首次答案、响应时间、解析阅读和回看；无论对错均在正文回填正确答案，错误显示暗红和局部解析，自动进入下一空格；三种诱导程度共用同一数据；刷新恢复。

### 阶段 5：学习完成页与局部解析

从真实 Attempt 计算空格数、首次正确率、错误步骤、知识/行为标签；局部解析包含正确原因、用户答案错误原因、知识点与下一题；支持同类短练习和无错误/无类题空态。

### 阶段 6：模拟测试引擎

实现科目、题量、范围、难度、薄弱点与时间模式设置；支持单选、多选、数值输入、题号导航、稍后检查、自动保存、交卷确认、刷新计时恢复与归零自动提交。作答中禁止答案、解析、得分和薄弱标签。

### 阶段 7：模拟结果与巩固生成

从真实答案统一判分，显示得分、用时、未答、超时、逐题结果与失分分类；基于规则生成真实存在的巩固题，避免立即重复与无限循环。

### 阶段 8：分析、错题本与历史

从 Attempt 动态计算初次正确率、完成率、平均响应时间、解析使用率、重复错误、掌握度、模拟正确率与用时；分离知识和解题行为；错题状态只能由后续表现推进；历史保留 session、revision 与必要快照。

### 阶段 9：排名与个人中心

实现明确标注的本地演示排名，用户成绩随真实本地 Attempt 更新；个人设置、数据量、时区与隐私说明可操作并持久化。真实联网排名和账号保持范围外。

### 阶段 10：本地内容管理

实现题目列表、JSON 导入/导出、Schema 定位、题目预览、学习与模拟共用渲染器；非法内容不得进入可用题库。

### 阶段 11：质量、可访问性与性能

完成键盘、焦点、语义、非纯颜色状态、48px 触控区、公式溢出、图片 alt、响应式、错误边界、空态和性能自查；补齐回归测试。

### 阶段 12：部署准备与最终验收

完善 README、内容指南、部署说明、环境约束与已知风险；清空状态执行全量 `check:all`；按需求矩阵逐项确认 `Verified`；输出最终 checkpoint，仅所有门禁 PASS 时完成。

### 阶段 13：中文/日文双语扩展

保留现有日语内容并新增全局 `日本語 / 中文` 切换；语言偏好持久化，页面、状态、无障碍名称和日期格式随语言变化。中文内置题库必须独立撰写题干、选项、解析、表格和图片说明，并与日文题库保持相同稳定 ID、revision、正确答案、分值与关联关系。门禁必须覆盖刷新持久化、答题中途切换不丢状态、学习/模拟/分析/排名/个人/管理页中文流程、双题库判分结构一致性和完整 `check:all`。

## 5. 完成定义

项目只有在所有 checkpoint 均 PASS、需求矩阵 P0/P1 全部 Verified、四个样例题通过数据校验、学习与模拟关键流程和刷新恢复通过 E2E、分析/错题/历史确由 Attempt 计算、非法题目导入被拒绝、生产构建成功后才算完成。
